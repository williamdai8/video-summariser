{
    "directories": {
        "base_output": "user_data",
        "logs": "user_data/logs",
        "video_output": "user_data/output/video",
        "image_output": "user_data/output/video_images",
        "temp_cli": "user_data/temp_cli_work",
        "temp_web": "user_data/temp_web_work",
        "temp_upload": "user_data/temp_web_upload"
    },
    "modes": {
        "_description": "Three modes for selecting video clips to include in the summary",
        "fixed": {
            "label": "Fixed Interval",
            "description": "📍 Selecting clips at fixed intervals (e.g., 10 seconds apart, 1 second duration)"
        },
        "random": {
            "label": "Random Interval",
            "description": "🎲 Randomly selecting clips with variable gaps and durations within specified ranges"
        },
        "coverage": {
            "label": "Target Coverage",
            "description": "🎯 Randomly selecting clips to achieve target coverage percentage of total video"
        }
    },
    "defaults": {
        "_comment": "mode can be 'fixed', 'random', or 'coverage'",
        "mode": "fixed",
        "chunk_size": 120,
        "interval": 10,
        "duration": 1.0,
        "min_gap": 5.0,
        "max_gap": 20.0,
        "min_clip": 1.0,
        "max_clip": 3.0,
        "coverage_percent": 20,
        "save_images": false
    },
    "logging": {
        "level": "INFO",
        "format": "%(asctime)s [%(levelname)s] %(message)s"
    }
}